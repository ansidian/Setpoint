# EA Dashboard

Claude-specific entry point. Keep this file deliberately thin so it does not drift from the shared agent instructions.

Read `AGENTS.md` first, then follow the linked source-of-truth docs from there.

Known MCP note: if `mcp__cclsp__find_workspace_symbols` reports cclsp "No Project", call diagnostics on any TypeScript file first, then retry.
